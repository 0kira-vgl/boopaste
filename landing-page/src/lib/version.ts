// Single source of truth for the version badge shown on the landing page
// and in the docs. Bump this by hand together with Cargo.toml's `version`
// on every release, so the site never lags behind what's actually
// published on GitHub Releases and Homebrew.
export const APP_VERSION = "0.1.1";
